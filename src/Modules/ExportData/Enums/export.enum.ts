export enum ExportFileType {
  CSV = "csv",
  EXCEL = "excel",
  WORD = "word",
}

export enum ExportEntityType {
  REQUISITIONS = "requisitions",
  ORDERS = "orders",
  LOGS = "logs",
  PRODUCTS = "products",
  SUPPLIERS = "suppliers",
}
