export enum ExportFileType {
  CSV = "csv",
  EXCEL = "excel",
}

export enum ExportEntityType {
  REQUISITIONS = "requisitions",
  ORDERS = "orders",
  LOGS = "logs",
  PRODUCTS = "products",
  SUPPLIERS = "suppliers",
}
